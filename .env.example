# Server-only. Never prefix this key with NEXT_PUBLIC_.
OPENAI_API_KEY=

# A fast structured-output model is the pragmatic default for batch screening.
# Override this without changing code if your account has a different model set.
OPENAI_MODEL=gpt-5.4-mini

